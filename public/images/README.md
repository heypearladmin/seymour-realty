# Brand assets

Drop the official files here using these exact filenames so the site picks them up automatically:

- `seymour-realty-logo.svg` — primary dark logo (currently a typographic placeholder; replace with the official lion + door-knocker mark)
- `seymour-realty-logo-light.svg` — same logo, light variant for dark backgrounds
- `laurel-seymour-headshot.jpg` — the editorial headshot of Laurel (used on the homepage intro and About page)

Recommended specs:

- Logo: SVG preferred. If only PNG is available, save as `seymour-realty-logo.png` and update `components/Navbar.tsx` and `components/Footer.tsx` accordingly.
- Headshot: JPG, ~2000px on the long edge, optimized. Aspect ratio 4:5 looks best in the editorial layout.
